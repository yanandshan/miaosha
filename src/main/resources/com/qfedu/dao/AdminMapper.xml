<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qfedu.dao.AdminMapper">
        <select id="login" parameterType="admin" resultType="int">
            select count(1) from user_info i,user_password p where i.telphone=#{username} and
             p.encrpt_password=#{password} and i.id = p.user_id;
        </select>
</mapper>